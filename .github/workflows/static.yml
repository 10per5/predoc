name: Deploy static content to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup

      - uses: ./.github/actions/build-editor
        with:
          live-url-base: /predoc
          editor-self-base: /predoc/editor-live/

      - uses: ./.github/actions/build-hugo
        with:
          baseurl: /predoc/
          editor-url: /predoc/editor-live

      - name: Copy editor into site output
        shell: bash
        run: |
          mkdir -p hugo-view/build/editor-live
          cp -r editor/public/. hugo-view/build/editor-live

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: hugo-view/build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
